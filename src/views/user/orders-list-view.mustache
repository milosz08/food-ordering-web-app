{{> partials/header-view.partial }}
<main class="container-lg flex-grow-1 top-margin-header">
    <nav aria-label="breadcrumb" class="ps-0">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="{{base_dir}}restaurants" class="text-secondary text-decoration-none">
                    <i class="bi bi-arrow-return-left me-1"></i>Powrót do strony wszystkich restauracji
                </a>
            </li>
        </ol>
    </nav>
    <hr/>
    {{> partials/dynamic-banner-view.partial}}
    {{#data.has_orders}}
    <h3 class="mb-1 fs-4 ps-0 mt-2">Moje ostatnie zamówienia</h3>
    <p class="mt-1 mb-4 text-secondary">Poniżej wyświetlana jest historia 10 ostatnich zamówień.</p>
    <div class="row mt-3">
        {{#data.orders}}
        <div class="col-md-6">
            <div class="card mb-3">
                <h5 class="card-header">{{name}}</h5>
                <div class="card-body">
                    <div class="row justify-content-between">
                        <div class="col-auto">
                            <h5 class="card-title fs-6">Nr zamówienia: {{id}}</h5>
                            <p class="fs-6 lh-sm">Status: <span class="{{order_status_color}}">{{order_status}}</span></p>
                            <p class="card-text fs-6">
                                <span class="text-primary small-font"><i class="bi bi-clock"></i> {{estimate_time}}</span>
                                <span class="coin-color small-font"><i class="bi bi-cash-coin ms-3"></i> {{price}}zł</span>
                            </p>
                            <a href="{{base_dir}}user/orders/order-details?id={{id}}" class="btn btn-sm btn-primary">Szczegóły zamówienia</a>
                        </div>
                        <div class="col-auto">
                            <img src="{{base_dir}}public/{{profile_url}}" class="img-fluid rounded" width="100" height="100" alt="">
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/data.orders}}
    </div>
    {{/data.has_orders}}
    {{^data.has_orders}}
    <div class="alert alert-warning mb-3" role="alert">
        Nie posiadasz na swoim koncie jeszcze żadnych zamówień. Aby dokonać pierwszego zamówienia, przejdź do strony wszystkich restauracji
        poprzez <a href="{{base_dir}}restaurants" class="alert-link">ten link</a>.
    </div>
    {{/data.has_orders}}
</main>
{{> partials/footer-view.partial }}

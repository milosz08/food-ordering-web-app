<h2 class="text-secondary fw-normal mb-3">Zarządzaj użytkownikami</h2>
<nav class="mb-4" aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{base_dir}}admin/dashboard">Panel główny</a></li>
        <li class="breadcrumb-item active" aria-current="page">Zarządzaj użytkownikami</li>
    </ol>
</nav>
<hr/>
{{> partials/dynamic-banner-view.partial}}
<div class="row justify-content-between align-items-center">
    <form class="col-lg-4 mb-3 d-flex justify-content-end" action="" method="post">
        <input type="search" name="search-user-name" placeholder="Szukaj po loginie lub mailu" class="form-control" value="{{data.search_text}}">
        <button type="submit" name="search-user-button" class="btn btn-primary btn-md ms-2 py-2">
            <i class="bi bi-search"></i>
        </button>
    </form>
</div>
{{#data.not_empty}}
<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Lp</th>
                <th>Login</th>
                <th>Email</th>
                <th class="d-none d-lg-table-cell">Dane adresowe</th>
                <th>Rola</th>
                <th class="d-none d-lg-table-cell">Konto</th>
                <th class="fit">Akcja</th>
            </tr>
        </thead>
        <tbody>
            {{#data.users_list}}
            <tr>
                <td class="align-middle">{{it}}</td>
                <td class="align-middle">{{login}}</td>
                <td class="align-middle">{{email}}</td>
                <td class="align-middle d-none d-lg-table-cell">{{address}}</td>
                <td class="align-middle">{{role}}</td>
                <td class="align-middle d-none d-lg-table-cell {{status.color_bts}}">
                    <span data-bs-toggle="tooltip" data-bs-placement="bottom" data-bs-title="{{status.tooltip_text}}" type="button">
                        {{status.text}}
                    </span>
                </td>
                <td class="align-middle fit flex-nowrap">
                    <button class="btn btn-danger ms-2 btn-sm" data-bs-toggle="modal" data-bs-target="#delete-user-modal-{{id}}">
                        <span data-bs-toggle="tooltip" data-bs-placement="bottom" data-bs-title="Usuń użytkownika" type="button">
                            <i class="bi bi-x-lg"></i>
                        </span>
                    </button>
                </td>
            </tr>
            <div class="modal fade" id="delete-user-modal-{{id}}" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
                aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h1 class="modal-title fs-5" id="staticBackdropLabel">Usuwanie użytkownika</h1>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            <p>Czy napewno chcesz usunąć użytkownika:</p>
                            <ul class="list-group list-group-flush">
                                <li class="list-group-item">Imię: <strong>{{first_name}}</strong></li>
                                <li class="list-group-item">Nazwisko: <strong>{{last_name}}</strong></li>
                                <li class="list-group-item">Login: <strong>{{login}}</strong></li>
                                <li class="list-group-item">Email: <strong>{{email}}</strong></li>
                                <li class="list-group-item">Adres: <strong>{{address}}</strong></li>
                                <li class="list-group-item">Rola: <strong>{{role}}</strong></li>
                                <li class="list-group-item">Status konta: <strong class="{{status.color_bts}}">{{status.text}}</strong></li>
                            </ul>
                            <p class="mt-3">
                                Po usunięciu użytkownika, skojarzone z nim adresy rónież zostaną usunięte. 
                                Operacji usunięcia użytkownika nie można cofnąć.
                            </p>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Zamknij</button>
                            <a class="btn btn-outline-danger" href="{{base_dir}}admin/manage-users/delete?id={{id}}">
                                Usuń
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            {{/data.users_list}}
        </tbody>
    </table>
</div>
{{> partials/pagination-view.partial}}
{{/data.not_empty}}
{{^data.not_empty}}
<div class="alert alert-warning mb-3" role="alert">
    Nie znaleziono żadnych użytkowników w bazie danych.
</div>
{{/data.not_empty}}
